# 🔐 TCC Full-Stack Authentication System

![Language](https://img.shields.io/badge/Language-C%23%20%7C%20TypeScript-blue)
![Framework](https://img.shields.io/badge/Framework-ASP.NET%20Core%20%7C%20Angular-red)
![Database](https://img.shields.io/badge/Database-PostgreSQL-brightgreen)
![Tests](https://img.shields.io/badge/Tests-Unit%20Testing-informational)

โปรเจกต์นี้คือระบบจัดการสมาชิกแบบครบวงจร (Full-Stack Authentication App) ที่รวบรวมโค้ดส่วน Front-end (Angular) และ Back-end (C# .NET Core API) สำหรับการจัดการผู้ใช้, การลงทะเบียน, การเข้าสู่ระบบ, และการเข้าถึงข้อมูลที่ได้รับการป้องกันด้วย JWT (JSON Web Token)

---

## ✨ Key Features (ฟังก์ชันหลัก)

* **Registration:** ลงทะเบียนผู้ใช้ใหม่ด้วยการเข้ารหัสรหัสผ่าน (Password Hashing)
* **Login & JWT:** การเข้าสู่ระบบและสร้าง JWT Token เพื่อใช้ในการยืนยันตัวตน
* **Secure Profile:** Endpoint ที่ได้รับการป้องกัน (Protected Route) เพื่อดึงข้อมูลโปรไฟล์ผู้ใช้
* **Security:** การตั้งค่า CORS และ JWT Bearer Authentication อย่างถูกต้อง
* **Code Quality:** Backend มีการ implement **Unit Test** เพื่อรับประกันความน่าเชื่อถือของ Business Logic

## 🛠️ Tech Stack

| ส่วนประกอบ | เทคโนโลยีหลัก | รายละเอียดการใช้งาน |
| :--- | :--- | :--- |
| **Backend API** | C# / ASP.NET Core 10.0+ | จัดการ API Endpoints (Controller-Based) และ Dependency Injection |
| **Database** | PostgreSQL | ฐานข้อมูลหลัก (ใช้ Entity Framework Core สำหรับ ORM) |
| **Testing** | **Unit Testing (xUnit)** | **ทดสอบ Business Logic ใน Service Layer** |
| **Frontend** | Angular (Latest) | User Interface, การจัดการ State, และการเรียกใช้ API |
| **Styling** | Angular CSS Modules | รูปแบบและ Layout ของหน้าจอ |

## 🏗️ Project Structure (โครงสร้างโปรเจกต์)

| โฟลเดอร์ | เทคโนโลยี | บทบาท |
| :--- | :--- | :--- |
| `frontend/` | Angular | User Interface (UI) และ Logic การโต้ตอบกับผู้ใช้ |
| `backend/` | C# .NET API | API Endpoints, Business Logic, การเชื่อมต่อ DB, และ Unit Tests |

---

## 🚀 Local Setup & Run (วิธีการตั้งค่าและรัน)

คุณต้องเปิด Terminal/Command Prompt สองหน้าต่างพร้อมกันเพื่อรันทั้งสองส่วนของแอปพลิเคชัน

### 1. การตั้งค่าและรัน Backend (C# .NET Core)

1.  **เข้าสู่โฟลเดอร์ Backend:**
    ```bash
    cd backend
    ```
2.  **เรียกคืน Dependencies และรันโปรเจกต์:** (API Server มักจะรันอยู่ที่ `http://localhost:5000`)
    ```bash
    dotnet restore
    dotnet run
    ```

### 2. การตั้งค่าและรัน Frontend (Angular)

1.  **เข้าสู่โฟลเดอร์ Frontend:**
    ```bash
    cd frontend
    ```
2.  **ติดตั้ง Dependencies (ครั้งแรก):**
    ```bash
    npm install
    ```
3.  **รันโปรเจกต์ Angular:**
    ```bash
    ng serve
    ```
    (Frontend จะรันอยู่ที่ `http://localhost:4200`)

### 🧪 การรัน Unit Tests

หากต้องการรัน Unit Tests ที่มีอยู่ในโปรเจกต์ Backend เพื่อตรวจสอบความถูกต้องของ Logic:

1.  **เข้าสู่โฟลเดอร์ Backend:**
    ```bash
    cd backend
    ```
2.  **รัน Unit Tests:**
    ```bash
    dotnet test
    ```

---

## 📸 Screenshots (ภาพหน้าจอ)

| หน้าจอ | คำอธิบาย |
| :--- | :--- |
| **Login Page** | หน้าจอสำหรับป้อน Username และ Password |
| **Register Page** | หน้าจอสำหรับสร้างบัญชีใหม่ |
| **Secure Profile** | หน้าจอที่แสดงข้อมูลที่ดึงมาจาก Protected Endpoint |

![หน้าจอ Login](assets/screenshots/login-page.png)
![หน้าจอ Register](assets/screenshots/register-page.png)
![หน้าจอ Profile](assets/screenshots/profile-page.png)
